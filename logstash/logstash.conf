input {
  tcp {
    port => 5000
    codec => json_lines
  }
}

output {
    if [type]=="logback" {
         elasticsearch {
             hosts => [ "elasticsearch:9200" ]
             index => "logback-%{+YYYY.MM.dd}"
        }
    }
}