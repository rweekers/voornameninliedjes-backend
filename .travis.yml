language: java
install: skip

os: linux
dist: xenial
jdk:  openjdk11

script:
- "./gradlew build --scan -s"
- "./gradlew jacocoTestReport"
- "./gradlew sonarqube"
addons:
  sonarcloud:
    organization: nl-orangeflamingo
