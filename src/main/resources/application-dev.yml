spring:
  flyway:
    locations: classpath:db/migration/{vendor}/prod,classpath:db/migration/{vendor}/dev
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8180/realms/voornameninliedjes

jobs:
  enrichSong:
    enabled: false
    rate: 60000
  updateSong:
    cron: "-"

logging:
  file:
    fail2ban: ./logs
